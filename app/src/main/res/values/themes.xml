<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BOOK_READING_APP" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.BOOK_READING_APP.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CustomDialogTheme" parent="Theme.Material3.Light.Dialog">
        <item name="android:windowBackground">@drawable/dialog_background</item>
        <item name="android:windowMinWidthMajor">100dp</item>
        <item name="android:windowMinWidthMinor">100dp</item>
    </style>
    <style name="Theme.BOOK_READING_APP" parent="Base.Theme.BOOK_READING_APP" />
    <style name="Theme.BOOK_READING_APP.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
</resources>